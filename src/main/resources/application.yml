server:
  port: 8761


spring:
  application:
    name: DemoEurekaServer

eureka:
  instance:
    hostname: localhost
  eureka:
    client:
      register-with-eureka: false
      fetch-registry: false
    server:
      wait-time-in-ms-when-sync-empty: 0